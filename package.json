{
  "name": "mkiexpertisecentrum",
  "version": "1.0.0",
  "scripts": {
    "build": "npx tailwindcss -i ./src/css/style.css -o ./_site/style.css && npx @11ty/eleventy",
    "start": "npx tailwindcss -i ./src/css/style.css -o ./_site/style.css --watch & npx @11ty/eleventy --serve",
    "test": "node tests/build.test.js"
  },
  "devDependencies": {
    "@11ty/eleventy": "^3.0.0",
    "autoprefixer": "^10.0.0",
    "postcss": "^8.0.0",
    "tailwindcss": "^3.0.0"
  }
}
