<!DOCTYPE html>
<html lang="nl">
  <head>
    <meta charset="UTF-8" />
    <title>{{ title or "MKI Expertisecentrum" }}</title>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <link href="/style.css" rel="stylesheet" />
    <link rel="icon" href="/img/logo.png" type="image/png" />
  </head>
  <body class="bg-gray-50 text-gray-900 font-sans antialiased leading-relaxed">
    {% include "header.njk" %}

    <main class="max-w-7xl mx-auto px-6 py-12">
      {{ content | safe }}
    </main>

    <footer class="border-t text-center text-sm text-gray-500 py-8 mt-16">
      © 2025 MKI Expertisecentrum — Alle rechten voorbehouden
    </footer>
  </body>
</html>
